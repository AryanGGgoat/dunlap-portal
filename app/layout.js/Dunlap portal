import "./globals.css";

export const metadata = {
  title: "Dunlap Portal",
  description: "Student Study Hub",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
